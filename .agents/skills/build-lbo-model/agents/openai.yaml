interface:
  display_name: "Build LBO Model"
  short_description: "Build control-transaction LBO returns models"
  default_prompt: "Use $build-lbo-model to assess suitability and produce an auditable Simplified Chinese LBO package."
policy:
  allow_implicit_invocation: false
